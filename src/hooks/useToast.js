/**
 * Custom hook to use toast notifications
 * Must be used within ToastProvider
 * 
 * Re-exports the useToast hook from ToastProvider for convenience
 */
export { useToast } from '../components/ui/ToastProvider';
